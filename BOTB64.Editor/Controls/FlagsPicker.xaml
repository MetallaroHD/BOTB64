<UserControl x:Class="BOTB64.Editor.Controls.FlagsPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Root"
             d:DesignWidth="220" d:DesignHeight="30">
    <StackPanel>
        <ToggleButton x:Name="Toggle"
                      Content="{Binding SummaryText, ElementName=Root}"
                      HorizontalContentAlignment="Left"
                      Padding="6,3"
                      MinWidth="180"/>
        <Popup IsOpen="{Binding IsChecked, ElementName=Toggle, Mode=TwoWay}"
               StaysOpen="False"
               AllowsTransparency="True"
               PlacementTarget="{Binding ElementName=Toggle}"
               Placement="Bottom">
            <Border Background="White" BorderBrush="#888888" BorderThickness="1" Padding="6" MaxHeight="280" MinWidth="180">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Options, ElementName=Root}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Popup>
    </StackPanel>
</UserControl>
